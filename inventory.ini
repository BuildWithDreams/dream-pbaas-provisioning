# Ansible Inventory
# ⚠️ Replace YOUR_TARGET_IP with your server's IP address before running
# All target-specific variables (IP, user, SSH key) are defined in group_vars/production.yml

[production]
135.181.136.105
